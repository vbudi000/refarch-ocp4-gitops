apiVersion: v1
kind: Service
metadata:
  name: {{ include "allinone.fullname" . }}
  labels:
    {{- include "allinone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "allinone.selectorLabels" . | nindent 4 }}
