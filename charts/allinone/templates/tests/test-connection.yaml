apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "allinone.fullname" . }}-test-connection"
  labels:
    {{- include "allinone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "allinone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
